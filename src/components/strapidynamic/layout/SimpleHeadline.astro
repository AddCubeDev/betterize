---
import type { SimpleHeadline } from "@components/strapidynamic/typescript/types";

interface Props {
    data: SimpleHeadline;
}

const { data } = Astro.props;
---

<div class="flex flex-col justify-center mx-auto">
    <h2 class="capitalize header-2">{data.text}</h2>
</div>
