---
interface Props {
    text: string;
    level: number;
}

const { text, level } = Astro.props;

function getHeader() {
    if (level >= 2 && level <= 6) {
        return `<h${level} class="blog-header mt-20">${text}</h${level}>`;
    } else {
        return `<h2 class="blog-header mt-20">Invalid header value given</h2>`;
    }
}

const htmlString = getHeader();
---

<Fragment set:html={htmlString} />
