---
import { Img } from "astro-imagetools/components";

const { blok } = Astro.props

// const {
//     alt = "descriptive text",
//     format = "avif",
//     title = "Tytuł komponentu AdvantageItem ...",
//     ...props
// } = Astro.props;

// if (blok.src === undefined || blok.src === null) {
//     console.error("Missing required 'src' attribute");
// }

// if (blok.width === undefined || blok.width === null) {
//     console.error("Missing required 'width' attribute");
// }
---

<div class="mb-4 border mx-auto max-w-5xl">
    <div class="pb-8">
        <Img
            src={blok.image.filename}
            alt="asdasda"
            placeholder="none"
            width={200}
            sizes="(max-width: 500px) 140px, 200px"
        />
    </div>
    <a href={blok.slug}>
        <time>{blok.date}</time>
        <h2 class="font-medium text-lg mb-2 title-font">
            {blok.title} 
        </h2>
        <p class="text-base">
            {blok.description}
        </p>
    </a>
</div> 